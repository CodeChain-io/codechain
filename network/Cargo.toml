[package]
name = "codechain-network"
version = "0.1.0"
authors = ["Kodebox <codechain@kodebox.io>"]

[dependencies]
codechain-types = { path = "../primitives/codechain-types" }
keccak-hash = { path = "../util/hash" }
rand = "0.4"
